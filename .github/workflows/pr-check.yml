name: PR-CHECK

on:
  pull_request: 
    branches: [ main ]
    type: [opened, synchronize]

jobs:
  pr-check:
    runs-on: ubuntu-latest

    steps:
      - name: check for pr in branch 
        run: echo "PR check running for branch: ${{ github.head_ref }}"
      
